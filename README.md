Running the 'npm i' command in the VS Code bash terminal after downloading the files, 
specifically the "package.json" file, 
will automatically install the dependencies listed in the "package.json" file
